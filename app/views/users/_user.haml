%tr
  %td
    = user.id
  %td
    = user.name
  %td
    = link_to '', edit_user_path(user.id), class: 'btn btn-default glyphicon glyphicon-pencil', title: 'Edit'
    = link_to '', user_path(user.id), method: :delete, class: 'btn btn-default glyphicon glyphicon-trash', title: 'Delete'